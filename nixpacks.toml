# Force PHP provider and specific version
[phases.setup]
nixPkgs = ["php81", "php81Extensions.mysqli", "php81Extensions.mysqlnd", "bash"]

# Clear build commands to prevent "npm" errors
[phases.build]
cmds = []

# Start PHP built-in server
[start]
cmd = "php -S 0.0.0.0:$PORT"
